{
  "name": "Python 3.11 + uv (on mcr image)",
  "image": "mcr.microsoft.com/devcontainers/python:3.11-bookworm",
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python", "ms-python.vscode-pylance", "ms-toolsai.jupyter"],
      "settings": {
        "python.defaultInterpreterPath": "${workspaceFolder}/1_fast_api_tutorial/.venv/bin/python",
        "python.terminal.activateEnvironment": true
      }
    }
  },
  "mounts": ["source=uv-cache,target=/root/.cache/uv,type=volume"],
  "onCreateCommand": "bash -lc 'set -euo pipefail; curl -LsSf https://astral.sh/uv/install.sh | sh; export PATH=\"$HOME/.local/bin:$PATH\"; uv sync --locked --project 1_fast_api_tutorial; uv sync --locked --project assignment'"
}
